{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "dependencies": {
    "socket.io": "1.4.4"
  },
  "engines": {
    "node": "4.x"
  },
  "scripts": {
    "preinstall": "./testScript.sh",
    "start": "test -z \"$VCAP_APPLICATION\" && node server.js || node --max_old_space_size=$(($(echo $VCAP_APPLICATION | jq '.limits.mem') - 5)) server.js"
  }
}
