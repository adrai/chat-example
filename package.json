{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "dependencies": {
    "gc-profiler": "1.3.1",
    "socket.io": "1.4.4",
    "usage": "0.7.1"
  },
  "engines": {
    "node": "4.x"
  },
  "scripts": {
    "preinstall": "./testScript.sh",
    "start": "test -z \"$VCAP_APPLICATION\" && node server.js || node --max-old-space-size=$(($(echo $VCAP_APPLICATION | jq '.limits.mem') - 25)) server.js",
  }
}
